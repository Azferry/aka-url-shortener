
locals {
  geo_short_codes = local.builtin_azure_region_geo_codes

  environment_short_codes = local.env_short_codes

  network_short_codes = local.net_short_codes

  tenant_short = local.tenant_short_codes
}